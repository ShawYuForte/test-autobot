(function (forte) {
    "use strict";

    function dashboardController($scope, $routeParams, $log) {

    }

    dashboardController.$inject = ['$scope', '$routeParams', '$log'];
    forte.app.controller("DashboardCtrl", dashboardController);

})(forte);